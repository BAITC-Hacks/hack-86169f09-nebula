# EventMatch AI

Рабочий репозиторий команды Nebula для трека 06 HackAlem AI: «Умный подбор подрядчиков».

## Простая идея

Пользователь выбирает город, дату, формат мероприятия, категорию подрядчика и бюджет.

Сервис показывает до трёх подходящих подрядчиков из выданного каталога и объясняет каждый выбор конкретными фактами:

- подрядчик работает в нужном городе;
- берёт нужный формат мероприятия;
- свободен в выбранную дату;
- его цена «от» укладывается в бюджет;
- при запросе проверяются язык и длительность.

Если подходящих нет, сервис объясняет причину: такой категории нет в городе или кандидаты не проходят условия.

## Границы задачи

В проект не входят бронирование, заявки подрядчикам, платежи и уведомления.

Цена в каталоге является ценой «от». Сервис не обещает окончательную стоимость услуги.

Данные каталога анонимизированы. Поля `synthetic`, `city_imputed` и `price_imputed` должны быть видны в карточке, когда они равны `true`.

## Роли команды

### Участник 1 — данные и алгоритм

- Добавляет исходный CSV в `data/`.
- Создаёт загрузчик и проверку структуры данных.
- Реализует фильтры: город, категория, формат, бюджет, дата, язык и длительность.
- Создаёт детерминированное ранжирование и объяснение причин пустой выдачи.
- Готовит тестовые сценарии из ТЗ.

### Участник 2 — AI Agent и NVIDIA Brev

- Поднимает на Brev HTTP-сервис агента и модельный endpoint.
- Реализует поток: понять запрос на обычном языке → уточнить обязательное поле при необходимости → вызвать инструмент `search_contractors` → проверить структурированный ответ → объяснить результат.
- Агент вызывает единый API поиска `/api/match`; он не придумывает подрядчиков и не меняет порядок ранжирования.
- Возвращает подтверждённые причины для карточек и причину пустого результата.
- Настраивает защищённый HTTPS-вызов Brev со стороны Vercel; секреты не попадают в браузер и GitHub.

### Участник 3 — generalist, интерфейс и Vercel

- Доводит форму и карточки до демо-сценария с текстовым запросом.
- Соединяет интерфейс с API агента и API поиска.
- Разворачивает приложение в Vercel из GitHub, проверяет Preview и Production URL.
- Обновляет README, фиксирует демо-запросы и готовит показ жюри.

## План разработки

### Этап 0. Старт проекта

Статус: сделано

- [x] Создана структура плана.
- [x] Зафиксирована задача и распределены роли.
- [x] Добавлен сайт: `index.html`, `styles.css`, `app.js`.
- [x] Добавлен контракт подключения данных: `data/README.md`.
- [ ] Каждый участник делает первый отдельный коммит в свой час работы.

### Этап 1. Данные и базовый поиск

Цель: по форме получить корректный список доступных кандидатов.

- [x] Добавлен исходный CSV как `data/contractors.csv`.
- [x] Добавить браузерный checkpoint: проверка 13 обязательных колонок перед подключением CSV.
- [x] Реализован checkpoint 13 колонок: ID, имя, категории, город, цена, форматы, языки, длительность, занятые даты, описание и флаги.
- [x] Реализованы строгие фильтры в `api/match.js`.
- [x] Занятый подрядчик исключается до ранжирования.
- [x] API возвращает не более трёх кандидатов.
- [x] Порядок фиксирован: score → цена → ID.

Готово, когда один и тот же запрос дважды возвращает одни и те же карточки в том же порядке.

### Этап 2. Объяснимый подбор

Цель: у каждой карточки есть собственное объяснение, основанное на каталоге.

- [ ] Ввести понятный расчёт ранга: обязательные условия + число дополнительных совпадений.
- [ ] Сформировать для каждой карточки 1–2 конкретных предложения.
- [ ] Упомянуть только подтверждённые поля профиля.
- [ ] Объяснить, почему кандидатов меньше трёх.
- [ ] Считать исключения по причинам: нет формата, выше бюджета, занят, нет языка, недостаточная длительность.

Пример хорошего объяснения: «Свободен 15 октября, ведёт корпоративы, говорит на русском и английском; цена от 1 000 000 ₸ укладывается в ваш бюджет».

### Этап 3. AI Agent на NVIDIA Brev

Цель: агент самостоятельно проводит ограниченный цикл поиска с инструментом, сохраняя правила трека.

- [ ] Поднять HTTP endpoint агента на Brev и проверить его доступность для Vercel по HTTPS.
- [ ] Принимать свободный текст; возвращать уточняющий вопрос, если не хватает обязательных полей.
- [ ] Вызвать инструмент `search_contractors`, связанный с `/api/match`.
- [ ] Использовать только результаты и причины, которые вернул каталог и matcher.
- [ ] При нулевой выдаче объяснять исключающие условия и предлагать возможные варианты изменения запроса.
- [ ] Не менять фильтры, карточки и порядок выдачи без явного ответа пользователя.
- [ ] Хранить ключ Brev в серверных переменных Vercel/Brev; не коммитить секреты.

### Этап 4. Интерфейс

Цель: жюри может самостоятельно выполнить поиск без помощи разработчика.

- [ ] Сделать форму с обязательными полями и понятной валидацией.
- [ ] Показать максимум три карточки.
- [ ] Показать «от» перед ценой.
- [ ] Выделить занятость как причину исключения.
- [ ] Добавить метки synthetic / восстановленный город / восстановленная цена.
- [ ] Сделать ссылку или кнопку для загрузки трёх демо-запросов.

### Этап 5. Обязательные демонстрации

- [ ] Плотная категория: Алматы, ведущий, корпоратив, 15.10.2026, бюджет 2 000 000 ₸.
- [ ] Тот же запрос на 25.12.2026: выдача должна измениться из-за занятости.
- [ ] Редкая категория: Алматы, флорист, свадьба, 15.10.2026, бюджет 1 000 000 ₸.
- [ ] В городе нет категории: Астана, декоратор.
- [ ] Кандидат есть, но не проходит условия: Астана, флорист, свадьба, 15.10.2026, бюджет 1 000 000 ₸.
- [ ] Запустить одинаковый запрос дважды и показать одинаковый порядок.

### Этап 6. Сдача

- [ ] README содержит архитектуру, установку, запуск и пять демо-запросов.
- [ ] В репозитории есть исходные данные или инструкция их получения.
- [ ] В `.env.example` описаны необязательные переменные.
- [ ] Проект запускается одной командой без личных аккаунтов проверяющего.
- [ ] В истории GitHub есть прогресс минимум один раз в час.
- [ ] Последний коммит сделан до дедлайна.

## Предлагаемая архитектура

```text
Форма поиска
      ↓
API поиска
      ↓
Проверка условий → причины исключения
      ↓
Детерминированное ранжирование
      ↓
AI-объяснение с проверкой фактов
      ↓
До 3 карточек или понятный пустой результат
```

## AI Agent: граница ответственности

Состояние на сейчас: API детерминированного подбора реализован в `api/match.js`; агентный endpoint на Brev и его подключение к сайту — следующий этап. Пока Brev не подключён, проект следует показывать как работающий matcher с интерфейсом, а не как завершённый агент.

Целевой поток:

```text
Браузер
  → Vercel: сайт и серверный gateway /api/agent
  → Brev: агент и модель
  → инструмент search_contractors
  → Vercel /api/match
  → data/contractors.csv и детерминированные правила
  → карточки и подтверждённое объяснение
```

Агент может извлечь параметры из текста и задать уточняющий вопрос. Отбор по городу, категории, формату, бюджету, занятости, языку и длительности выполняет matcher. Агент не создаёт карточки, не меняет порядок результатов и не снимает ограничения молча. При пустом результате он показывает фактическую причину и предлагает варианты для следующего запроса.

Секрет доступа к Brev задаётся только в серверных переменных проекта Vercel, например `BREV_AGENT_URL` и `BREV_AGENT_TOKEN`. Не добавляйте секрет в `app.js`, HTML, публичные переменные окружения или коммиты. Для текущего rule-based приложения переменные не требуются.

## Деплой в Vercel

Репозиторий GitHub остаётся главным источником кода. Vercel публикует сайт и API; он не заменяет GitHub. Подключайте Vercel к репозиторию `BAITC-Hacks/hack-86169f09-nebula`.

### Настройки проекта

- Root Directory: корень репозитория.
- Framework Preset: Other.
- Build Command: пусто.
- Output Directory: пусто / значение по умолчанию.
- Install Command: пусто; приложение не использует npm-пакеты.
- Переменные окружения: пока не нужны для обычного поиска.

Файл `vercel.json` включает CSV в серверную функцию `api/match.js`. Файл `server.js` используется для локального запуска; Vercel автоматически публикует содержимое `/api` как Node.js Functions, а корневые HTML/CSS/JS-файлы — как сайт.

После подключения GitHub проверьте Preview URL перед Production. Проверьте главную страницу, загрузку каталога и `/api/match`: ответ должен содержать `catalog_profiles: 66` и одно из состояний `matched`, `category_not_found` или `no_match`. Повтор одинакового запроса должен возвращать карточки в том же порядке. Демо-сценарии есть в форме.

### Дальнейшие изменения

После готовности Brev добавьте `BREV_AGENT_URL` и `BREV_AGENT_TOKEN) в Project Settings → Environment Variables Vercel, затем создайте новый deployment. Секреты не добавляйте в README или GitHub. Публичная ссылка на Vercel deployment будет зафиксирована здесь после проверки доступности сайта и API.

## Как запустить локально

Нужен Node.js 20 или новее.

```powershell
node server.js
```

Откройте `http://localhost:3000`. Сервер отдаёт интерфейс, CSV и API `/api/match` без установки библиотек и без личного аккаунта.

## Текущая архитектура

- `data/contractors.csv` — официальный каталог из 66 профилей.
- `api/match.js` — серверный подбор по правилам ТЗ.
- `app.js` — интерфейс, который обращается к API; при загрузке своего CSV использует локальный безопасный fallback.
- `server.js` — локальный запуск сайта и API одной командой.
- `index.html` и `styles.css` — пользовательский интерфейс.

## Технологии

- Node.js без внешних зависимостей для локального запуска;
- CSV как единственный источник фактов о подрядчиках;
- HTML/CSS/JavaScript для интерфейса;
- NVIDIA Brev — планируемое место запуска AI Agent и модельного сервиса;
- Vercel — веб-интерфейс, публичный endpoint и серверный gateway к Brev;
- детерминированный matcher остаётся единственным источником результатов и порядка карточек;
- если Brev временно недоступен, интерфейс сохраняет обычный поиск по правилам.

## Ссылки

- [Техническое задание](https://docs.google.com/document/d/1rhR2HFY164BrnkIP39N3usY9fNY4JeAgkPxEzbqL00w/edit)
- [Каталог подрядчиков CSV](https://drive.google.com/file/d/1uUCu-szctwaTaV0-Yfg3FKHY8M3lQ3vw/view)
- [Страница HackAlem AI](https://edu.astanahub.com/hackathons/df4743f5-c492-415c-b45a-1f13adb78e06?tab=tracks)
